import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  fonts: {
    body: 'Noto Sans JP, Open Sans, sans-serif',
    heading: 'Noto Sans JP, Open Sans, serif',
    mono: 'JetBrains Mono, monospace',
  }
})
